namespace Enigma_Simulator
{
    public partial class Plugboard : Reflector
    {
        //technically plugboard and reflector has the same logic, therfore i can just use it.
        public Plugboard()
        {
            InitializeComponent();
        }
    }
}
